<br><br><br><h6>Sightings for {{}}</h6>
<p>Here are your sightings</p>
{{#if sightings}}
    <table class="striped">
        <thead>
            <tr class="headers">
                <th>Timestamp</th>
                <th>Lights on?</th>
            </tr>
        </thead>
        <tbody class="table-contents">
            {{#each sighting}}
            <tr>
                <td>{{createdAt}}</td>
                <td>{{lightsOn}}</td>
            </tr>
            {{/each}}
        </tbody>
    </table>
{{else}}
    <p>You haven't recorded any sightings yet.</p>

{{/if}}